This is the server (serverless!) part of the lane swimming site.

To run (watch) unit tests:

`test:unit:watch`

To run integration tests:

`npm run test:integration`

To view AWS logs:

`npm run logs`
